BEGIN TRY

BEGIN TRAN;

ALTER TABLE [dbo].[Promocion] ADD [destacadaEnHome] BIT NOT NULL CONSTRAINT [Promocion_destacadaEnHome_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
